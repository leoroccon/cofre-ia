[Unit]
Description=Cofre de IA (Django + gunicorn)
After=network.target

[Service]
User=cofre
Group=cofre
WorkingDirectory=/home/cofre/app
ExecStart=/home/cofre/app/.venv/bin/gunicorn cofre.wsgi:application --bind 127.0.0.1:8000 --workers 2
Restart=on-failure

[Install]
WantedBy=multi-user.target
